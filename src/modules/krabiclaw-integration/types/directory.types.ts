export interface KrabiClawOrganizationDirectoryRecord {
  id: string;
  name: string;
  slug: string;
}

export interface KrabiClawUserDirectoryRecord {
  id: string;
  name: string;
  email: string;
}
